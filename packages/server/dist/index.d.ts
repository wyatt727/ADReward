import express from 'express';
/**
 * Start the ADReward REST API server
 * @param port Port number to listen on
 * @returns Express server instance
 */
export declare function startServer(port?: number): Promise<express.Express>;
